fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
pandas>=2.1.0,<3.0.0
pdfplumber>=0.10.0,<1.0.0
anthropic>=0.40.0,<1.0.0
slowapi>=0.1.9,<1.0.0

# ML column classifier (Phase 3)
scikit-learn>=1.4.0          # RandomForest column type classifier
sentence-transformers>=2.6   # Merchant normalizer (all-MiniLM-L6-v2)
psutil>=5.9.0                # Memory check before loading ML models

# Multi-AI router providers (install as needed per environment)
# groq>=0.9.0           — Groq free tier (14,400 req/day, Llama 70B quality)
#                          pip install groq   |   GROQ_API_KEY=xxx
# google-generativeai>=0.8.0 — Gemini free tier (1,500 req/day, 1M tok/day)
#                          pip install google-generativeai   |   GEMINI_API_KEY=xxx
# Ollama: runs as a local server, no pip install needed
#   Download: https://ollama.com   |   OLLAMA_URL=http://localhost:11434
