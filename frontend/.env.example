# Backend API URL (set in Vercel environment variables)
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# Pro Report Quality Gate — set to "true" to enable Claude QA checks
NEXT_PUBLIC_REPORT_QA_ENABLED=false

# Anthropic API key — required only if REPORT_QA_ENABLED=true (server-side only)
ANTHROPIC_API_KEY=sk-ant-...
